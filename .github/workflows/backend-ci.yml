name: backend-ci

on:
  push:
    paths:
      - "backend/**"
      - ".github/workflows/backend-ci.yml"
  pull_request:
    paths:
      - "backend/**"
      - ".github/workflows/backend-ci.yml"

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: AWS login
        uses: ./.github/actions/aws-login
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to ECR
        run: |
          aws ecr get-login-password --region ${{ secrets.AWS_REGION }} \
            | docker login --username AWS --password-stdin 622711946516.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: |
            backend/requirements.txt
            backend/test-requirements.txt

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r test-requirements.txt

      - name: Lint (ruff)
        run: ruff check .

      - name: Format check (black)
        run: black --check .

      - name: Import order check (isort)
        run: isort --check-only .

      - name: Tests (pytest)
        run: pytest tests/

      - name: Docker build
        run: |
          IMAGE_TAG=${GITHUB_SHA::7}
          docker build -t deployment_tool:${IMAGE_TAG} .
          docker tag deployment_tool:${IMAGE_TAG} 622711946516.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/deployment_tool:${IMAGE_TAG}

      - name: Docker push
        run: |
          IMAGE_TAG=${GITHUB_SHA::7}
          docker push 622711946516.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/deployment_tool:${IMAGE_TAG}
